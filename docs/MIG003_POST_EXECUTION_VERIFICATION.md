# MIG-003 — Post-Execution Verification

**Phase:** MIG-003. **Result: PASS.** Full machine-readable detail: `supabase/audit/MIG003_POST_EXECUTION_COMPARISON.json`.

## Fresh post-execution capture

- Path: `MIG-001_LOCAL_CAPTURE/mig003_post_execution_schema_capture.csv` (outside every Git repository; not committed)
- Generated by: the project owner, running the exact unmodified `MIG001_READ_ONLY_SCHEMA_CAPTURE.sql` manually, immediately after execution. Claude Code made no connection to Supabase.
- SHA-256: `736032512a879a6680ae03e934bf96dfe6e9b09a7ef468c11afb0215edbfce23`
- Structure: valid, 26 top-level keys, same 11-table set, same Postgres version string as every prior capture. Bounded secret scan: zero matches.
- Notably smaller than the pre-execution capture (220,401 bytes vs. 331,346 bytes) — consistent with the expected reduction in `table_privileges`/`column_privileges` rows following the `REVOKE`.

## Structural invariants: all 16 categories IDENTICAL pre → post

| Category | Result |
|---|---|
| tables, columns, constraints, indexes, triggers, functions, policies, generated_columns, enums, domains, sequences, views, materialized_views, schemas, extensions, publication_membership | **IDENTICAL**, every one |

**Zero non-privilege structural change occurred.** No schema, table, column, index, constraint, function, trigger, or RLS policy was created, dropped, or altered by the grant execution — confirmed by direct comparison of the full captured definitions, not merely by counting objects.

## Privilege verification — exact match to the target matrix on all 22 (table, role) pairs

| Table | anon | authenticated (post) | authenticated (target) | Result |
|---|---|---|---|---|
| `articles`, `authors`, `journals`, `specialties`, `subtopics`, `article_specialties` | NONE | `SELECT` | `SELECT` | MATCH (×6) |
| `interaction_signals` | NONE | `DELETE, INSERT, SELECT` | `DELETE, INSERT, SELECT` | MATCH |
| `user_preferences` | NONE | `INSERT, SELECT, UPDATE` | `INSERT, SELECT, UPDATE` | MATCH |
| `article_processing_log`, `api_cache`, `content_fingerprints` | NONE | NONE | NONE | MATCH (×3) |

**Zero mismatches.** Independently corroborated via a second, unrelated catalogue mechanism — the capture's `data_api_exposure` section, computed via `has_table_privilege()` rather than `information_schema.table_privileges` — which agrees exactly: `anon_*` is `false` for every privilege on every one of the 11 tables; `authenticated_*` is `true` only where the target matrix specifies, and `false` everywhere else, including all four checked privileges on `article_processing_log`, `api_cache`, and `content_fingerprints`.

## `article_processing_log` special case — confirmed correct

The stale RLS policy `processing_log_authenticated_read` (`SELECT`, `USING (auth.role() = 'authenticated')`) is present, byte-identical, in both the pre- and post-execution captures — **untouched, as required.** Its continued presence does not grant `authenticated` any access, because the table privilege it depends on was revoked and not re-granted; this is confirmed directly in the privilege table above (`authenticated`: NONE) and via `data_api_exposure` (`authenticated_select: false`).

## `service_role`: unaffected

77 raw privilege rows before and after execution, byte-identical set. `service_role_select: true` on every table in the post-execution `data_api_exposure` section, matching the worker's documented direct-Postgres-connection design (unaffected by this Data API grant change either way).

## Overall result

**PASS.** Every criterion in the MIG-003 authorisation's pass-condition list is satisfied: the reviewed SQL executed successfully; the fresh post-capture is valid; the target client-role privilege matrix matches exactly; `service_role` is unchanged; no unexpected structural, RLS, or policy change exists; no unexplained drift appears anywhere in the comparison.

**Rollback is not required and was not performed.**
