# MIG-003 — Pre-Execution Verification and Drift Gate

**Phase:** MIG-003 — Controlled Production Grant Execution and Independent Post-Execution Verification
**Date:** 2026-08-30
**Status of this document:** read-only preflight and drift analysis only. **No production mutation has occurred.** Nothing in this document authorises execution — that requires the project owner's separate, explicit `AUTHORIZE MIG-003 PRODUCTION GRANT EXECUTION` message.

## Repository/artifact verification

All four repositories independently re-verified before proceeding:

| Repo | Branch | HEAD | Status |
|---|---|---|---|
| Perspectives_Prototype | `main` | `5c2a2f6b08b0f145e2ee0c1cfa23637ab62a7ef3` | unchanged, clean |
| Perspectives-app | `main` | `b46136e6f909b5be5bab7c959e8eeafa5900f9b8` | unchanged, clean |
| Perspectives-worker | `audit/mig-003-production-grants` (new, based on `audit/mig-002-local-replay`) | base `cba58025bd0e0b691dfe3881925a7f9b87cb0e77` | clean |
| Base44-to-standalone | `audit/mig-002-local-replay` (untouched — no MIG-003 change here yet) | `b190efa1f1eff41baa308a972d2d8b575f2c82f9` | clean |

All MIG-001 (#2) and MIG-002 (#3) pull requests in both repositories confirmed **OPEN, MERGEABLE**. No merge, no history rewrite, no partial MIG-003 work found anywhere.

## Reviewed execution artifacts (exact bytes, unmodified)

| Artifact | Path | Commit | SHA-256 |
|---|---|---|---|
| **Target grant SQL** | `supabase/grants/MIG002_REVIEWED_GRANT_PLAN.sql` | `cba58025bd0e0b691dfe3881925a7f9b87cb0e77` | `c1fb20bac47386f8e4402f66718db556f2f9a43100e6b6c92fe332e329088303` |
| **Rollback SQL** | `supabase/grants/MIG002_REVIEWED_GRANT_ROLLBACK.sql` | `cba58025bd0e0b691dfe3881925a7f9b87cb0e77` | `5a8b47d23157f1fd562e9041d6e111cb559091ebc4d2d680a7b632276f808cf3` |
| Target access matrix | `supabase/baseline/MIG002_TARGET_ACCESS_MATRIX.csv` | `cba58025bd0e0b691dfe3881925a7f9b87cb0e77` | `f7b1c4a97c6a9b7a1ca1c75a8a0fbc3d78b4358225899bdd0e745311f8d91d5a` (33 rows) |
| Capture query (for the fresh capture) | `supabase/audit/MIG001_READ_ONLY_SCHEMA_CAPTURE.sql` | `2cf8f43c19d3806a477a1fc7552a9bc7c961bbe6` | `c42c0292235f8a30809779d6fbb6e43e392413910ab46ee98f256bbd369be828` |

These hashes were re-verified against the currently-committed files immediately before this report was written — the bytes reviewed and locally rehearsed in MIG-002 are the exact bytes that would be proposed for production. **No artifact was modified for this phase.**

## Original historical capture (immutable reference)

- Path: `MIG-001_LOCAL_CAPTURE/live_schema_capture.csv` (outside every Git repository, untouched throughout)
- SHA-256: `670f0cfd82dbf783d7891663c58924a420137ee2b0014fdc3474de417b1fb7e1` — re-verified unchanged.

## Fresh pre-execution capture

- Path: `MIG-001_LOCAL_CAPTURE/mig003_pre_execution_schema_capture.csv` (outside every Git repository; **not committed, never will be**)
- Generated by: the project owner, running the exact unmodified `MIG001_READ_ONLY_SCHEMA_CAPTURE.sql` manually in the live PERSPECTIVES Supabase SQL Editor. Claude Code made no connection to Supabase.
- SHA-256: `40f67d5e40fef08a599ed1864f01aa59503de4f9a0aabd698657da369876507f`
- Structure: valid CSV, 1 header + 1 data row, column `mig001_schema_capture`, valid JSON, exactly 26 top-level keys, exactly 11 public tables (`api_cache`, `article_processing_log`, `article_specialties`, `articles`, `authors`, `content_fingerprints`, `interaction_signals`, `journals`, `specialties`, `subtopics`, `user_preferences`) — identical set to the historical capture.
- Bounded secret scan (function/policy/constraint/trigger/column-default text): **zero matches.**
- `postgres_version`: identical string to the historical capture (`PostgreSQL 17.6 on aarch64-unknown-linux-gnu, compiled by gcc (GCC) 15.2.0, 64-bit`).

## Drift comparison: historical (MIG-001) vs. fresh (MIG-003 pre-execution)

Full machine-readable detail: `supabase/audit/MIG003_PRE_EXECUTION_DRIFT.json`.

**Result: 225/225 comparisons EXACT_MATCH. Zero drift of any kind.**

| Category | Compared | Result |
|---|---|---|
| Public table set | 11 tables | EXACT_MATCH |
| Table flags (RLS enabled/forced, owner, comment) | 11 | 0 mismatches |
| Columns | 114 | 0 mismatches |
| Constraints | 32 | 0 mismatches |
| Indexes | 35 | 0 mismatches |
| Triggers | 4 | 0 mismatches |
| Functions | 1 | 0 mismatches |
| RLS policies | 13 | 0 mismatches — **`article_processing_log`'s stale `processing_log_authenticated_read` policy confirmed still present, unmodified**, exactly as MIG-003's scope requires (grant cleanup only, no RLS change) |
| **`anon`/`authenticated`/`service_role` table privileges** | 231 rows (154 + 77) | **EXACT_MATCH — the live pre-transition state is byte-identical to what MIG-002's target plan was built and rehearsed against** |
| Data API exposure (`has_table_privilege` per table) | 11 tables | 0 mismatches |
| Schema-level USAGE/CREATE privileges | 4 roles | EXACT_MATCH |

**No unexplained structural, RLS, policy, or grant drift exists.** The only difference between the two captures is the `query_executed_at` timestamp, which is expected and not evaluated as a comparison item.

## Current production privilege summary (pre-execution)

- `anon`: full `SELECT/INSERT/UPDATE/DELETE/TRUNCATE/REFERENCES/TRIGGER` on all 11 tables (77 rows) — the legacy over-permissive default, unchanged since MIG-001.
- `authenticated`: identical shape (77 rows) — same legacy default.
- `service_role`: full privileges on all 11 tables (77 rows) — Supabase's standard default, not targeted for change by MIG-003.

This exactly matches the "Stage 7 — Verified pre-grant state" documented in `docs/MIG002_LOCAL_GRANT_VALIDATION.md`, confirming the target grant plan's assumptions about the *starting* state remain valid today.

## MIG-002 target-assumption validity check

| Assumption | Still valid? |
|---|---|
| 11 tables exist, named as expected | Yes |
| `anon`/`authenticated` hold the full legacy grant on all 11 tables (the state the plan's `REVOKE ALL` step assumes) | Yes |
| `article_processing_log` has exactly one policy, `processing_log_authenticated_read`, `SELECT`-only, `auth.role() = 'authenticated'` | Yes |
| The six public-read tables' policies are unchanged (`SELECT`-only, `auth.role() = 'authenticated'`) | Yes |
| `interaction_signals`/`user_preferences` policy command sets are unchanged (no `UPDATE` on the former, no `DELETE` on the latter) | Yes |
| `api_cache`/`content_fingerprints` remain zero-policy (deny-all) | Yes |
| `service_role` privileges unaffected by anything since MIG-001 | Yes |
| Rollback SQL's assumed pre-state (full legacy grant, 154 rows) matches current reality | Yes |

**Every MIG-002 target assumption remains valid against the live database today.**

## GO / NO-GO recommendation

**GO.**

No unexplained drift, structural or otherwise, exists between the MIG-001 historical capture and this fresh pre-execution capture. All reviewed artifacts are confirmed byte-identical to what was locally rehearsed and verified in MIG-002. The drift gate is satisfied.

**This report does not authorise execution.** Per the MIG-003 authorisation, production SQL execution requires a separate, explicit `AUTHORIZE MIG-003 PRODUCTION GRANT EXECUTION` message from the project owner, after which the manual, user-operated execution procedure in Part 4 of that authorisation begins.
